# SIH Demo

Status: **NOT YET BUILT — Phase 16 (SIH Demo Mode) has not started.**

This file will contain, once the three demo scenarios (PRD.md section 13)
are implemented and rehearsed:

- exact setup steps for each scenario (real voice / cloned voice / human
  scammer)
- demo reset procedure
- fallback plan if network/live inference is unavailable
- the judge-facing explanation script

Rules.md section 17 applies: the demo must be reproducible, and a
pre-recorded result must never be silently substituted while being
presented as live inference.
